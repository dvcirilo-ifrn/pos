# Unidade 02 - Lista 01

1. Quais são os métodos HTTP usados em APIs REST? Relacione cada um com a ação (criar, ler, etc.).
1. O que significa dizer que uma API REST é *stateless*? Cite outros dois princípios do REST.
1. O que é um *API endpoint*? Dê um exemplo de endpoint para listar e outro para atualizar um recurso.
1. O que é uma mensagem SOAP? Quais são as partes obrigatórias?
1. Para que serve o WSDL em um serviço SOAP?
1. O que faz a biblioteca Zeep e qual problema ela resolve ao consumir uma API SOAP em Python?
1. O que diferencia autenticação de autorização?
1. Quais são as diferenças entre Basic Auth, API Key e OAuth? Dê um exemplo de uso de cada uma.
1. No fluxo OAuth, para que servem o Client ID, o Client Secret e o Redirect URI?
1. O que é a Fetch API e para que ela é usada em um cliente JavaScript?
1. O que é uma *Promise* em JavaScript? Quais são seus três estados possíveis?
1. O que faz o *bundling* e por que ferramentas como o Vite.js são usadas no desenvolvimento front-end?
1. Como fazer, com a Fetch API, uma requisição com um método diferente de GET (ex. PUT), incluindo headers e body?
1. Qual a diferença entre consumir uma Promise com `.then()`/`.catch()` e com `async`/`await`? Dê um exemplo de cada.
1. O que são módulos em JavaScript (ESM)? Como funcionam os comandos `export` e `import`?
